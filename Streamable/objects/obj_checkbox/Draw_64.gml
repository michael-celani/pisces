/// @description Insert description here
var index = active;

if (active)
{
	index += checked();	
}

draw_roundrect_ext(x, y, x + 49, y + 49, 25, 25, false);
//draw_sprite_stretched(spr_checkbox, index, x, y, 50, 50);
